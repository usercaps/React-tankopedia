import React from "react";

const {
    Provider: TankopediaServiceProvider,
    Consumer: TankopediaServiceConsumer,
} = React.createContext();

export { TankopediaServiceProvider, TankopediaServiceConsumer };
